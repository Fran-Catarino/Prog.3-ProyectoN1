import React, { Component } from "react";

class Plantilla extends Component {
    constructor(props){
        super(props)
        this.state = {
            /* <!-- value: '' Para forms --> */
        }
    }
    render(){
        return(
            <div>
            </div>
        )
    }
}

export default Plantilla;